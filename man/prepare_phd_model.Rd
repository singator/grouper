% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_prep.R
\name{prepare_phd_model}
\alias{prepare_phd_model}
\title{Prepare the PhD workload allocation model}
\usage{
prepare_phd_model(
  df_list,
  t_max_y1 = 1,
  e_max = NULL,
  ta_min = NULL,
  ta_max = NULL,
  gr_min = NULL,
  gr_max = NULL,
  e_min = NULL,
  alpha = 2,
  beta = 1,
  phi = 1,
  rho = 10,
  C = 4
)
}
\arguments{
\item{df_list}{A list of model inputs, typically from \code{\link[=extract_phd_info]{extract_phd_info()}}.
Required elements are:
\itemize{
\item \code{Ns}: number of students
\item \code{Nj}: number of courses
\item \code{P}: preference matrix \code{[i, j]}
\item \code{d}: demand matrix \code{[j, r]} where \code{r = 1:3} for TA, GR, E
\item \code{s}: seniority vector (offset form; e.g. year - 2)
\item \code{t1}: past TA workload vector
\item \code{g1}: past GR workload vector
}}

\item{t_max_y1}{Maximum current-semester TA load for Year-1 students
(\code{s == -1}) before slack is used.}

\item{e_max}{Optional upper bound on per-student E units in current semester.}

\item{ta_min, ta_max}{Optional lower/upper bounds on per-student TA units in
current semester.}

\item{gr_min, gr_max}{Optional lower/upper bounds on per-student GR units in
current semester.}

\item{e_min}{Optional lower bound on per-student E units in current semester.}

\item{alpha}{Objective weight on TA spread \code{(Tmax - Tmin)}.}

\item{beta}{Objective weight on TA preference term.}

\item{phi}{Objective weight on seniority-weighted E term.}

\item{rho}{Objective weight on Year-1 TA slack penalties.}

\item{C}{Semester workload capacity per student. The model fixes annual
workload at \code{2 * C} via \code{T_i + G_i + e_i^(2) == 2 * C}.
Default is \code{4}.}
}
\value{
An \code{ompr} model object ready for \code{ompr::solve_model()}.
}
\description{
Builds a mixed-integer optimisation model for assigning TA, GR, and E units
across students and courses.
}
\details{
Index alignment is critical: \code{P[i, j]}, \code{d[j, ]}, \code{s[i]},
\code{t1[i]}, and \code{g1[i]} must refer to the same student/course ordering.
}
